---
phase: 02.1
plan: "04"
subsystem: notes-ui
tags: [html, css, notes, midnight-glass, ui-skeleton]
dependency_graph:
  requires: [02.1-01, 02.1-02]
  provides: [notes-dashboard-html, notes-css]
  affects: [app/templates/index.html, app/static/css/app.css]
tech_stack:
  added: []
  patterns: [midnight-glass-overlay, masonry-grid, glass-card-tints, css-pulse-animation]
key_files:
  created: []
  modified:
    - app/templates/index.html
    - app/static/css/app.css
decisions:
  - "Notes icon button placed in RSB collapsed rail (#right-rail) after finance button — matches existing rsb-rail-icon pattern"
  - "notes-dashboard uses z-index 150 — between finance panel (z-index 200) and main content"
  - "Card color tints use literal rgba() values (not CSS variables) for consistent rendering — same approach as Chart.js colors"
  - "Close button uses #icon-close SVG reference — existing symbol in defs block"
metrics:
  duration_seconds: 127
  completed_date: "2026-03-19"
  tasks_completed: 2
  files_modified: 2
---

# Phase 02.1 Plan 04: Notes HTML Skeleton and CSS Summary

Notes dashboard HTML structure added to index.html and all notes UI CSS appended to app.css — visual skeleton ready for Plan 05's JS wiring.

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Add notes HTML structure to index.html | 1a700d2 | app/templates/index.html |
| 2 | Add notes CSS (grid, cards, 6 tints, animations) | 62987a7 | app/static/css/app.css |

## What Was Built

**Task 1 — HTML skeleton (index.html):**
- `<symbol id="icon-notes">` SVG symbol in defs block (notepad with document lines, 24x24)
- `#btn-notes-open` button in `#right-rail` (collapsed RSB icon strip) with `#notes-badge` amber count span
- `#notes-dashboard` full-screen overlay with:
  - `.notes-header` — title "Notes" + close button
  - `#notes-grid` — grid container for note cards
  - `#notes-create-card` — dashed create card (first in grid) with placeholder and hidden textarea
  - `#notes-empty` — hidden empty state div with voice tip text

**Task 2 — CSS (418 lines appended to app.css):**
- Notes dashboard: `position: fixed; inset: 0; z-index: 150; background: rgba(0,0,0,0.95)`
- Notes grid: `grid-template-columns: repeat(auto-fill, minmax(250px, 1fr))` with ghost scrollbar recipe
- Note card base: `border-radius: 12px`, backdrop-filter glass recipe, hover box-shadow elevation
- 6 card color tints (literal rgba values from UI-SPEC.md):
  1. `card-color-default` — rgba(15,15,15,0.5)
  2. `card-color-deep-blue` — rgba(30,58,138,0.35)
  3. `card-color-dark-teal` — rgba(20,84,92,0.35)
  4. `card-color-amber-night` — rgba(120,53,15,0.35)
  5. `card-color-rose-night` — rgba(108,28,55,0.35)
  6. `card-color-mauve-glass` — rgba(76,29,149,0.35)
- `card-overdue-pulse` with `@keyframes pulse-overdue` (amber box-shadow 1.5s infinite)
- Card controls (trash icon, hover-reveal via `display:flex`)
- Inline edit textarea with accent focus ring
- Create card (dashed border, editing state class)
- Empty state (absolute centered)
- Notes badge: amber `#F59E0B`, absolute top-right on `#btn-notes-open`
- Undo delete toast: glass recipe + `@keyframes toast-slide-up`
- Responsive breakpoints: tablet (≤1079px) and mobile (≤767px)

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check

### Files Created/Modified
- [x] app/templates/index.html — modified (35 lines added)
- [x] app/static/css/app.css — modified (418 lines added)

### Commits
- [x] 1a700d2 — feat(02.1-04): add notes dashboard HTML structure to index.html
- [x] 62987a7 — feat(02.1-04): add complete notes CSS to app.css

## Self-Check: PASSED
